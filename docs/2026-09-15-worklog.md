# Blip worklog — 2026-09-15 (Asia/Jakarta)

## 2026-09-15 00:00:00 +07:00

- Inspected `docs/README.md` and confirmed `/home/zam/Projects/blip` is the project root.
- Confirmed the repository was empty apart from the project brief; no existing implementation was overwritten.
- Added the Rust package manifest and dependency set in `Cargo.toml`.
- Implemented `src/main.rs`: centralized TOML config, CLI commands (`serve`, `validate`, `history`), webhook routing, provider authentication, event/branch filtering, bounded serial queue, lock-file overlap prevention, fire-and-forget/tracked execution, timeout support, and JSONL deploy history.
- Added `blip.example.toml` with a GitLab example and `docs/systemd/blip.service` for native systemd deployment.
- Added `docs/USAGE.md` with build, validation, serving, history, and webhook configuration instructions.

## Verification

- `cargo check` initially reached dependency resolution but failed because the sandbox could not resolve `index.crates.io`.
- Network access was then approved for dependency download; compilation exposed an unstable `str::as_str()` usage.
- Replaced that usage with stable `&str` comparison. A final `cargo check` should be run after this edit.
- `cargo fmt` completed successfully.
- Final `cargo check` completed successfully with no errors.

## 2026-09-15 00:05:00 +07:00

- Final verification completed; implementation files and documentation are present in the project root.

## 2026-09-15 00:10:00 +07:00

- Prepared Git workflow support files: `.gitignore`, local configuration template, and safe test deploy script.
- Added `docs/TESTING-GITLAB.md` for the requested `feature/blip-webhook-mvp` → `dev` merge test flow, including all commands for the operator to run.
- Normalized GitLab's `Push Hook` header to the configured `push` event so the merge-to-`dev` test is accepted.

## 2026-09-15 00:15:00 +07:00

- Verified configured Git identity: `zamkara <zamkara@gnuweeb.org>`.
- Verified SSH authentication to GitLab using `~/.ssh/gitlab`; GitLab confirmed account `@zamkara`.
- Initialized the local Git repository, created commit `3685e12`, configured remote `git@gitlab.com:almateraincubator/utilities/blip.git`, and pushed branch `feature/blip-webhook-mvp` successfully.

## 2026-09-15 23:41:02 +07:00

- Created ignored local `blip.toml` from the local testing template; webhook secret remains a placeholder until the GitLab webhook is configured.
- Confirmed `scripts/test-deploy.sh` is executable.
- Built the optimized release binary successfully with `cargo build --release`.
- Validated local configuration successfully: 1 project configured.
- Ran the safe local deploy test script successfully; it wrote `.blip-test-deploy.log`, which is excluded by `.gitignore`.
- Working tree remains clean because local runtime configuration and generated files are ignored.

## 2026-09-15 23:50:00 +07:00

- Reviewed the supplied GitLab webhook form and current official GitLab webhook documentation.
- Confirmed signing-token behavior: `whsec_` + base64 32-byte key; signature uses `webhook-id`, `webhook-timestamp`, and raw body.
- Added preferred `signing_token`, optional legacy `secret`, constant-time comparison, and five-minute replay protection.

## 2026-09-15 23:55:00 +07:00

- Added `docs/LOCAL-SETUP.md` as the pre-webhook device preparation checklist, including branch sync, build/test, local config, safe script test, endpoint test, and final webhook steps.
